apiVersion: v1
kind: Secret
metadata:
  name: docker-credentials
data:
  # IMPORTANT: do not commit real registry credentials.
  # This value should be base64(config.json) from your local Docker config.
  config.json: REPLACE_WITH_BASE64_DOCKER_CONFIG_JSON
